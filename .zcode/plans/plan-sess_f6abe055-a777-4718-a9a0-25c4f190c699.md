## Consolidate scholarship hunt to a single permission

**Goal:** `can_use_scholarship_hunt` becomes the one and only permission gating all scholarship hunt features. Remove `can_use_scholarship_analyze` and `can_use_scholarship_deep_hunt` as separate limits — from enforcement code, seed data, migrations, UI, and dead code.

**Jira:** Will create `SCHOLARDOCX-0136` in `Epic-ScholarshipHunt` (or `Epic-BillingAndPlans`).

---

### 1. Backend — redirect the two enforcement helpers (2 files)

**`backend/app/api/scholarship_opportunities.py`** (`_require_scholarship_analyze`, ~line 33):
- Change `check_and_increment_limit(user, "can_use_scholarship_analyze", ...)` → `"can_use_scholarship_hunt"`
- Change `feature_plan_phrase("can_use_scholarship_analyze", ...)` → `"can_use_scholarship_hunt"`
- Update the error message wording from "Analyze is available on…" → "Scholarship Hunt is available on…"

**`backend/app/api/scholarship_deep_hunt.py`** (`_require_scholarship_deep_hunt_access`, ~line 67):
- Change `check_and_increment_limit(user, "can_use_scholarship_deep_hunt", ...)` → `"can_use_scholarship_hunt"`
- Change `feature_plan_phrase("can_use_scholarship_deep_hunt", ...)` → `"can_use_scholarship_hunt"`
- Update the docstring + error message wording to reference Scholarship Hunt.

The routes stay alive and callable (per your choice), now gated by the single permission.

### 2. Backend — prune seed/baseline data (3 files)

**`backend/app/db/schema.py`** — remove these 8 lines (2 keys × 4 roles):
- `can_use_scholarship_analyze`: free_user L25, general_user L45, pro_user L65, max_user L85
- `can_use_scholarship_deep_hunt`: free_user L26, general_user L46, pro_user L66, max_user L86

**`backend/app/db/connection.py`**:
- Delete the `scholarship_analyze_permission_defaults` block (L526–539).
- Delete the `scholarship_deep_hunt_permission_defaults` block (L541–556).
- Remove the two keys from `free_user_defaults` (L591–592).
- **Remove** `"can_use_scholarship_analyze"` and `"can_use_scholarship_deep_hunt"` from `canonical_features` (L657–658). This is what purges orphaned rows from existing user DBs on next init.

**`backend/app/services/admin.py`** (`DEFAULT_ROLE_LIMITS`):
- Remove `can_use_scholarship_analyze` and `can_use_scholarship_deep_hunt` lines from all 4 user-role blocks (free, general, pro, max) including the duplicate free_user block.

### 3. Backend — update tests (2 files)

**`backend/tests/test_limits_billing_guards.py`** (~L303–330): the `test_permission_only_check_commits_bootstrap_row` test uses `can_use_scholarship_deep_hunt`. Rewrite it to use `can_use_scholarship_hunt` (the test's purpose is the transaction-isolation behavior, not the specific key). Update the comment + the `usage_count` assertion accordingly.

**`backend/tests/test_news_service.py`**: already uses `can_use_scholarship_hunt` — no change needed (confirmed by research).

### 4. Frontend — remove the two keys from display/config (3 files)

**`frontend/src/components/PlanComparisonView.tsx`** — delete the `can_use_scholarship_deep_hunt` row from `extendedFeatures` (L189). (`can_use_scholarship_hunt` in `coreFeatures` stays as the single visible row.)

**`frontend/src/components/admin/RoleLimitsTab.tsx`** — delete the `can_use_scholarship_deep_hunt` entry from the "Scholarship Hunt" feature group (L216).

**`frontend/src/lib/accessErrors.ts`** — delete the `can_use_scholarship_analyze` and `can_use_scholarship_deep_hunt` label entries (L10–11).

### 5. Frontend — delete dead Deep Hunt code (3 files)

Per your choice, delete:
- `frontend/src/components/news/DeepHuntView.tsx`
- `frontend/src/lib/scholarshipDeepHuntApi.ts`
- `frontend/src/components/news/deep-hunt.css`

(All confirmed orphaned — `DeepHuntView` is not imported anywhere; its API client is its sole consumer; the CSS only it used.)

### 6. What I will NOT touch

- The `source="scholarship_hunt"` **billing-ledger label** in `news.py:86` and the admin usage stats (`admin.py:248,250,289`) — that's billing telemetry, not a permission.
- The Deep Hunt backend service/model/routes — kept alive, just re-gated (per your choice).
- `App.tsx` — already only reads `can_use_scholarship_hunt`, no change needed.
- `UsageModal.tsx` — auto-hides removed keys via its dynamic `can_` matching; no edit needed.

### Verification

- `grep -rn "can_use_scholarship_analyze\|can_use_scholarship_deep_hunt" backend/ frontend/src/` → expect zero hits in source (only possibly in dist/ artifacts).
- `cd backend && python -m pytest -q` → all green.
- `cd frontend && npm run build` → success.
- `grep -rn "DeepHuntView\|scholarshipDeepHuntApi" frontend/src/` → zero hits.

### AI-Context updates

- Update the story completion notes with all changed files.
- Note in `technical/security-privacy.md` (or the relevant RBAC context) that scholarship hunt is now gated by a single permission, if that doc lists the old keys.